{
  "name": "tins2026-client",
  "version": "0.1.0",
  "license": "Zlib",
  "private": true,
  "description": "Vite + TypeScript + Phaser 3 client for the TINS 2026 game-jam starter kit. Connects to the authoritative Socket.IO server; renders synced entities behind the shared IRenderer interface.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "cap:sync": "npm run build && npx cap sync",
    "android:add": "npx cap add android",
    "android:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.4",
    "@capacitor/core": "^8.3.4",
    "phaser": "^3.88.2",
    "socket.io-client": "^4.8.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.3.4",
    "@types/node": "^20.14.0",
    "typescript": "^5.4.0",
    "vite": "^5.4.0"
  }
}
