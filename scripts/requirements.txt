# No third-party dependencies. The Suno pipeline uses only the Python standard library
# (urllib, json, argparse, os, shutil, pathlib, time) so a clean clone needs no pip install.
# Requires Python 3.8+.
